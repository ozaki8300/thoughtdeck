====================
FILE: lib/useDeckCore.ts
====================

import type { Area, ThemeMode } from "./deckTypes";
import { getTitle, isH1, isH2, isH3 } from "./deckParser";

export type ParsedCard = {
  id: string;
  area: Area;
  title: string;
  lines: string[];
  source: "raw";
  visualGroup: number;
  rawH3Index: number;
  areaBlockIndex: number;
};

export type AddedCard = {
  id: string;
  area: Area;
  title: string;
  lines: string[];
  source: "added";
  visualGroup?: number;
};

export type Card = ParsedCard | AddedCard;

export type OneColumnSection = {
  id: string;
  title: string;
  lines: string[];
  position: "top" | "bottom";
  visualGroup: number;
};

export type TextPanelFocusItem = {
  id: "td-memo" | "td-output";
  title: string;
  lines: string[];
  focusKind: "memo" | "output";
  focusLabel: string;
};

export type FocusItem =
  | (Card & { focusKind: "card"; focusLabel: string })
  | (OneColumnSection & { focusKind: "section"; focusLabel: string })
  | TextPanelFocusItem;

export type ParsedDeck = {
  title: string;
  topSections: OneColumnSection[];
  cards: ParsedCard[];
  bottomSections: OneColumnSection[];
};

export const THOUGHTDECK_HOME_URL = "/";

export const defaultQuickLinks = [
  { label: "ThoughtDeck", url: "/" },
  { label: "GLOBIS VC", url: "https://vc.globis.ac.jp/wam/auth/login" },
] as const;

export const PDF_MIN_WIDTH = 360;
export const PDF_DEFAULT_WIDTH = 760;
export const themeLabel = (mode: ThemeMode) => mode === "auto" ? "自動" : mode === "light" ? "ライト" : "ダーク";
export const nextThemeMode = (mode: ThemeMode): ThemeMode => mode === "auto" ? "light" : mode === "light" ? "dark" : "auto";

export const placeholderSets = [
  {
    label: "発散",
    left: "何が起きている？",
    center: "どう考える？",
    right: "何が論点？",
  },
  {
    label: "再構造化",
    left: "どう見える？",
    center: "なぜそう思う？",
    right: "どこで使える？",
  },
  {
    label: "自由",
    left: "",
    center: "",
    right: "",
  },
] as const;

export const blankRaw = `# タイトル（例：投資判断の整理）

## ISSUE
- ここに入力

### 事実
@area: left
- ここに入力

### 解釈
@area: center
- ここに入力

### 論点
@area: right
- ここに入力

## まとめ
- ここに入力
`;

export const demoRaw = `# なぜ思考は消えてしまうのか？

## ISSUE
- なぜ、良い議論や学びは後から再現できなくなるのか？
- なぜ、メモを書いても「何を考えていたのか」を思い出せないのか？
- どうすれば、思考を**構造**として残せるのか？

### ThoughtDeckがない世界
@area: left
- メモが断片化する
- 気づきが埋もれる
- 論点を見失う
- 学びが流れていく
- 後から読んでも思い出せない
- 思考が再利用できない

### 思考の限界
@area: center
- 情報と論点が混ざる
- 頭の中だけでは構造を維持できない
- 考えている途中で整理が崩れる
- 「**何を考えるべきか**」が見えなくなる
- 良い議論や気づきが、その場で消えていく

### ThoughtDeckで変わること
@area: right
- 思考を「**構造**」として残せる
- 論点を可視化できる
- 考えた過程を共有できる
- 後から再び思考に戻れる
- 点だった学びがつながっていく
- 思考が次の思考を生む

## まとめ
- 考えた過程を残せる
- 後から再び思考に戻れる
- 論点を共有できる
- 学びがつながっていく
- 良い議論を、その場で終わらせない
`;

export const demoOutput = `これまでのメモは、
「情報を残す」ことはできても、
「**何を考えていたのか**」を残すことは難しかった。

ThoughtDeckでは、
素材・思考・示唆を分けて**構造化**することで、
思考そのものを可視化できる。

その結果、
後から再び思考に戻ることができ、
学びや議論を積み上げられるようになる。

ThoughtDeckは、
**考えること**
**共有すること**
**思い出すこと**
をつなぐ workspace である。`;

export function normalizeDisplayLines(lines: string[]) {
  return lines.map((line) => line.trim()).filter(Boolean);
}


export function areaFromLine(line?: string): Area {
  const rawArea = line?.replace(/^@area:/, "").trim();
  return rawArea === "left" || rawArea === "center" || rawArea === "right"
    ? rawArea
    : "center";
}

export function parseDeck(raw: string): ParsedDeck {
  const lines = raw.split("\n");
  const title = getTitle(raw);
  const topSections: OneColumnSection[] = [];
  const bottomSections: OneColumnSection[] = [];
  const cards: ParsedCard[] = [];

  let currentSection: OneColumnSection | null = null;
  let currentCard: ParsedCard | null = null;
  let hasSeenCard = false;
  let visualGroup = 0;
  let h3Index = -1;
  let areaBlockIndex = 0;
  let currentCardTitle = "見出し";

  const flushSection = () => {
    if (!currentSection) return;
    currentSection.lines = normalizeDisplayLines(currentSection.lines);
    if (currentSection.position === "top") topSections.push(currentSection);
    else bottomSections.push(currentSection);
    currentSection = null;
  };

  const flushCard = () => {
    if (!currentCard) return;
    currentCard.lines = normalizeDisplayLines(currentCard.lines).filter(
      (line) => !line.startsWith("@area:"),
    );
    cards.push(currentCard);
    currentCard = null;
  };

  const hasCardBody = (card: ParsedCard | null) =>
    Boolean(card && card.lines.some((line) => line.trim().length > 0));

  const createRawCard = (area: Area = "center"): ParsedCard => ({
    id: `raw-${h3Index}-${areaBlockIndex}-${currentCardTitle}`,
    source: "raw",
    area,
    title: currentCardTitle,
    lines: [],
    visualGroup,
    rawH3Index: h3Index,
    areaBlockIndex,
  });

  for (const line of lines) {
    const trimmed = line.trim();

    if (trimmed === "---") {
      flushCard();
      flushSection();
      visualGroup += 1;
      continue;
    }

    if (isH1(line)) {
      flushCard();
      flushSection();
      continue;
    }

    if (isH2(line)) {
      flushCard();
      flushSection();
      const position: "top" | "bottom" = hasSeenCard ? "bottom" : "top";
      currentSection = {
        id: `${position}-${position === "top" ? topSections.length : bottomSections.length}-${trimmed}`,
        title: trimmed.replace(/^##[ \t\u3000]*/, "").trim() || "セクション",
        lines: [],
        position,
        visualGroup,
      };
      continue;
    }

    if (isH3(line)) {
      flushSection();
      flushCard();
      hasSeenCard = true;
      h3Index += 1;
      areaBlockIndex = 0;
      currentCardTitle = trimmed.replace(/^###[ \t\u3000]*/, "").trim() || "見出し";
      currentCard = createRawCard("center");
      continue;
    }

    if (currentCard) {
      if (trimmed.startsWith("@area:")) {
        const nextArea = areaFromLine(trimmed);

        // 1つの「### 見出し」の中に @area が複数ある場合は、
        // @area ごとにカードを分割する。
        // これにより、見出しは同じまま left / center / right に自然に展開できる。
        if (hasCardBody(currentCard)) {
          flushCard();
          areaBlockIndex += 1;
          currentCard = createRawCard(nextArea);
        } else {
          currentCard.area = nextArea;
        }
        continue;
      }

      currentCard.lines.push(line);
      continue;
    }

    if (currentSection) {
      currentSection.lines.push(line);
    }
  }

  flushCard();
  flushSection();

  return { title, topSections, cards, bottomSections };
}

export function getActiveAreaEntries(block: { left: Card[]; center: Card[]; right: Card[] }) {
  return ([
    ["left", block.left],
    ["center", block.center],
    ["right", block.right],
  ] as const).filter(([, items]) => items.length > 0);
}

export function getDynamicColumnClass(count: number) {
  if (count <= 1) return "grid-cols-1";
  if (count === 2) return "grid-cols-2";
  return "grid-cols-3";
}

export function getAreaPlaceholder(perspective: (typeof placeholderSets)[number], area: Area) {
  if (area === "left") return perspective.left;
  if (area === "center") return perspective.center;
  return perspective.right;
}
